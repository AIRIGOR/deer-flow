config_version: 29
log_level: info

models:
  - name: rigor-analyzer
    display_name: RIGOR Analyzer
    use: langchain_openai:ChatOpenAI
    model: $RIGOR_MODEL
    api_key: $RIGOR_MODEL_API_KEY
    base_url: $RIGOR_MODEL_BASE_URL
    temperature: 0
    max_retries: 2

sandbox:
  use: deerflow.sandbox.local:LocalSandboxProvider
  allow_host_bash: false
